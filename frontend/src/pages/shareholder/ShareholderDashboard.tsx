import { DashboardLayout } from "../DashboardLayout";

export function ShareholderDashboard() {
  return (
    <DashboardLayout title="Shareholder Dashboard" badge="View & download only">
      <p>
        This is where shareholders will view and download company documents —
        share certificates, AGM notices, annual returns — once those features
        are built.
      </p>
    </DashboardLayout>
  );
}
