import { DashboardLayout } from "../DashboardLayout";

export function DirectorDashboard() {
  return (
    <DashboardLayout title="Director Dashboard" badge="View & download only">
      <p>
        This is where directors will view and download company documents —
        board minutes, resolutions, filings — once those features are built.
      </p>
    </DashboardLayout>
  );
}
