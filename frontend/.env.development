# Empty on purpose: the frontend calls relative paths like "/api/auth/login",
# and Vite's dev server proxy (see vite.config.ts) forwards those to the
# backend on localhost:4000. This keeps everything on one origin, which is
# what makes sharing a single tunnel/public URL work.
VITE_API_URL=""
